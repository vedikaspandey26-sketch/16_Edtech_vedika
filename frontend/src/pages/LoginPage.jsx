import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Sparkles, GraduationCap, KeyRound } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export default function LoginPage() { const [name, setName] = useState('Ava Sharma'); const [email, setEmail] = useState('demo@studymate.ai'); const [password, setPassword] = useState('studymate123'); const [error, setError] = useState(''); const { login } = useAuth(); const navigate = useNavigate(); const submit = async e => { e.preventDefault(); try { await login(name, email, password); navigate('/') } catch (err) { setError(err.message) } }; const demo = () => { setName('Ava Sharma'); setEmail('demo@studymate.ai'); setPassword('studymate123') }; return <div className="max-w-5xl mx-auto px-6 py-16 grid md:grid-cols-2 gap-12 items-center"><div><div className="w-16 h-16 rounded-card bg-indigo-soft/15 text-indigo-soft flex items-center justify-center"><GraduationCap size={32}/></div><h1 className="font-display text-4xl font-semibold mt-6">Your study space awaits.</h1><p className="text-text-secondary leading-relaxed mt-4">Sign in to open your personal home, discover resources, take quizzes, and track your progress.</p><div className="mt-8 card p-5 text-sm text-text-secondary"><div className="flex gap-3"><KeyRound className="text-teal-soft shrink-0"/><span><strong className="text-text-primary">Demo profile</strong><br/>Email: demo@studymate.ai<br/>Password: studymate123</span></div><button type="button" className="btn-secondary !py-2 !px-4 text-sm mt-4" onClick={demo}>Reset demo details</button></div></div><form onSubmit={submit} className="card p-7"><h2 className="font-display text-xl font-semibold">Sign in to StudyMate</h2><label className="block text-sm mt-6 mb-2">Name</label><input className="input-field" value={name} onChange={e => setName(e.target.value)} required/><label className="block text-sm mt-4 mb-2">Email</label><input type="email" className="input-field" value={email} onChange={e => setEmail(e.target.value)} required/><label className="block text-sm mt-4 mb-2">Password</label><input type="password" className="input-field" value={password} onChange={e => setPassword(e.target.value)} required/>{error && <p className="text-coral text-sm mt-3">{error}</p>}<button className="btn-primary mt-6 w-full">Enter my study space</button><p className="text-xs text-text-tertiary mt-3 text-center">Demo authentication only — not production security.</p></form></div> }
